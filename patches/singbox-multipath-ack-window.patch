diff --git a/protocol/multipath/core.go b/protocol/multipath/core.go
index 368c32e..17b1885 100644
--- a/protocol/multipath/core.go
+++ b/protocol/multipath/core.go
@@ -123,6 +123,7 @@ type wireFrame struct {
 
 type replayEntry struct {
 	frame          wireFrame
+	writeStartedAt time.Time
 	sentAt         time.Time
 	fallbackQueued bool
 	acked          bool
@@ -322,6 +323,7 @@ type mpCore struct {
 	ackNext      atomic.Uint64
 	ackedNext    atomic.Uint64
 	ackWake      chan struct{}
+	txWindowWake chan struct{}
 	replayMu     sync.Mutex
 	replay       map[uint64]*replayEntry
 	replayBytes  int64
@@ -364,19 +366,20 @@ func newCore(parent context.Context, cfg coreConfig) (*mpCore, net.Conn) {
 	ctx, cancel := context.WithCancel(context.WithoutCancel(parent))
 	appConn, txPipe, rxPipe := newLogicalPipe()
 	c := &mpCore{
-		cfg:      cfg,
-		ctx:      ctx,
-		cancel:   cancel,
-		appConn:  appConn,
-		txPipe:   txPipe,
-		rxPipe:   rxPipe,
-		legs:     make(map[uint8]*mpLeg),
-		reserved: make(map[uint8]bool),
-		incoming: make(chan wireFrame, cfg.QueueFrames*2),
-		done:     make(chan struct{}),
-		activeCh: make(chan struct{}),
-		ackWake:  make(chan struct{}, 1),
-		replay:   make(map[uint64]*replayEntry),
+		cfg:          cfg,
+		ctx:          ctx,
+		cancel:       cancel,
+		appConn:      appConn,
+		txPipe:       txPipe,
+		rxPipe:       rxPipe,
+		legs:         make(map[uint8]*mpLeg),
+		reserved:     make(map[uint8]bool),
+		incoming:     make(chan wireFrame, cfg.QueueFrames*2),
+		done:         make(chan struct{}),
+		activeCh:     make(chan struct{}),
+		ackWake:      make(chan struct{}, 1),
+		txWindowWake: make(chan struct{}, 1),
+		replay:       make(map[uint64]*replayEntry),
 	}
 	appConn.onClose = func() { c.fail(io.EOF) }
 	c.bufferPool.New = func() any {
@@ -592,6 +595,9 @@ func (c *mpCore) putBuffer(buffer []byte) {
 
 func (c *mpCore) txLoop() {
 	for {
+		if err := c.waitSendWindow(); err != nil {
+			return
+		}
 		buffer := c.getBuffer()
 		n, err := c.txPipe.Read(buffer[:c.cfg.ChunkSize])
 		if n > 0 {
@@ -627,6 +633,35 @@ func (c *mpCore) txLoop() {
 	}
 }
 
+func (c *mpCore) sendWindowFrames() uint64 {
+	// ACKs describe the receiver's next contiguous sequence. Bound the number
+	// of later frames that can be emitted so one slow leg cannot make the other
+	// leg overflow the receiver's reorder limits.
+	frames := c.cfg.QueueFrames
+	if c.cfg.MaxReorderFrames < frames {
+		frames = c.cfg.MaxReorderFrames
+	}
+	if byteFrames := int(c.cfg.MaxReorderBytes / int64(c.cfg.ChunkSize)); byteFrames < frames {
+		frames = byteFrames
+	}
+	if frames < 1 {
+		frames = 1
+	}
+	return uint64(frames)
+}
+
+func (c *mpCore) waitSendWindow() error {
+	limit := c.sendWindowFrames()
+	for c.txSeq.Load()-c.ackedNext.Load() >= limit {
+		select {
+		case <-c.done:
+			return errCoreClosed
+		case <-c.txWindowWake:
+		}
+	}
+	return nil
+}
+
 func (c *mpCore) sendFIN() error {
 	if !c.localFIN.CompareAndSwap(false, true) {
 		return nil
@@ -865,7 +900,7 @@ func (c *mpCore) legWriteLoop(leg *mpLeg) {
 			leg.queuedBytes.Add(-length)
 			leg.writingBytes.Add(length)
 			if leg.id == 1 {
-				c.markReplaySent(frame.seq)
+				c.markReplayWriteStarted(frame.seq)
 			}
 			err := writeWireFrame(leg.conn, frame)
 			leg.writingBytes.Add(-length)
@@ -873,6 +908,9 @@ func (c *mpCore) legWriteLoop(leg *mpLeg) {
 				c.legFailed(leg, legFailureWriteData, err)
 				return
 			}
+			if leg.id == 1 {
+				c.markReplaySent(frame.seq)
+			}
 			c.legCounters[leg.id].txBytes.Add(uint64(length))
 			c.legCounters[leg.id].txFrames.Add(1)
 			if leg.id == 0 {
@@ -1145,9 +1183,17 @@ func (c *mpCore) untrackReplay(seq uint64, release bool) {
 	}
 }
 
+func (c *mpCore) markReplayWriteStarted(seq uint64) {
+	c.replayMu.Lock()
+	if entry := c.replay[seq]; entry != nil && entry.writeStartedAt.IsZero() {
+		entry.writeStartedAt = time.Now()
+	}
+	c.replayMu.Unlock()
+}
+
 func (c *mpCore) markReplaySent(seq uint64) {
 	c.replayMu.Lock()
-	if entry := c.replay[seq]; entry != nil && entry.sentAt.IsZero() {
+	if entry := c.replay[seq]; entry != nil {
 		entry.sentAt = time.Now()
 	}
 	c.replayMu.Unlock()
@@ -1167,6 +1213,10 @@ func (c *mpCore) handleACK(next uint64) {
 			break
 		}
 	}
+	select {
+	case c.txWindowWake <- struct{}{}:
+	default:
+	}
 	var buffers [][]byte
 	c.replayMu.Lock()
 	for seq, entry := range c.replay {
@@ -1206,19 +1256,41 @@ func (c *mpCore) replayLoop() {
 		case <-c.done:
 			return
 		case now := <-ticker.C:
-			stalled := false
+			writeStalled := false
+			sequences := make([]uint64, 0)
 			c.replayMu.Lock()
-			for _, entry := range c.replay {
-				if !entry.fallbackQueued && !entry.sentAt.IsZero() && now.Sub(entry.sentAt) >= c.cfg.ReplayTimeout {
-					stalled = true
-					break
+			for seq, entry := range c.replay {
+				if entry.fallbackQueued || entry.writeStartedAt.IsZero() {
+					continue
+				}
+				if entry.sentAt.IsZero() {
+					if now.Sub(entry.writeStartedAt) >= c.cfg.ReplayTimeout {
+						writeStalled = true
+						break
+					}
+					continue
+				}
+				if now.Sub(entry.sentAt) >= c.cfg.ReplayTimeout {
+					entry.fallbackQueued = true
+					sequences = append(sequences, seq)
 				}
 			}
+			if writeStalled {
+				// Full-leg recovery owns every outstanding entry. Undo partial
+				// selection so reinjectLeg1 cannot skip frames from this scan.
+				for _, seq := range sequences {
+					c.replay[seq].fallbackQueued = false
+				}
+			}
 			c.replayMu.Unlock()
-			if stalled {
+			if writeStalled {
 				if leg := c.getLeg(1); leg != nil {
 					c.legFailed(leg, legFailureReplay, errLeg1Stalled)
 				}
+				continue
+			}
+			if len(sequences) > 0 {
+				go c.queueReplayFallback(sequences)
 			}
 		}
 	}
@@ -1234,6 +1306,10 @@ func (c *mpCore) reinjectLeg1() {
 		}
 	}
 	c.replayMu.Unlock()
+	c.queueReplayFallback(sequences)
+}
+
+func (c *mpCore) queueReplayFallback(sequences []uint64) {
 	sort.Slice(sequences, func(i, j int) bool { return sequences[i] < sequences[j] })
 	ticker := time.NewTicker(time.Millisecond)
 	defer ticker.Stop()
